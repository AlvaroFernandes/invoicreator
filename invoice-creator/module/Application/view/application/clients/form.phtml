<?php
/** @var array|null $data */
/** @var int|null $id */
?>
<form method="post">
    <div class="mb-3">
        <label class="form-label">Company Name</label>
        <input name="company_name" class="form-control" required value="<?= $this->escapeHtml($data['company_name'] ?? '') ?>">
    </div>
    <div class="mb-3">
        <label class="form-label">Company Contact</label>
        <input name="company_contact" class="form-control" value="<?= $this->escapeHtml($data['company_contact'] ?? '') ?>">
    </div>
    <div class="mb-3">
        <label class="form-label">ABN</label>
        <input name="abn" class="form-control" value="<?= $this->escapeHtml($data['abn'] ?? '') ?>">
    </div>
    <div class="mb-3">
        <label class="form-label">Contact Email</label>
        <input name="contact_email" type="email" class="form-control" value="<?= $this->escapeHtml($data['contact_email'] ?? '') ?>">
    </div>
    <div class="mb-3">
        <label class="form-label">Contact Phone</label>
        <input name="contact_phone" class="form-control" value="<?= $this->escapeHtml($data['contact_phone'] ?? '') ?>">
    </div>
    <div class="mb-3">
        <label class="form-label">Address</label>
        <textarea name="address" class="form-control"><?= $this->escapeHtml($data['address'] ?? '') ?></textarea>
    </div>
    <div class="d-flex gap-2">
        <button class="btn btn-primary" type="submit"><?= isset($id) ? 'Save' : 'Create' ?></button>
        <a class="btn btn-secondary" href="<?= $this->url('clients') ?>">Cancel</a>
    </div>
</form>
