<?php
/** @var array $clients */
?>
<div class="d-flex justify-content-between align-items-center mb-3">
    <h2>Clients</h2>
    <a href="<?= $this->url('clients', ['action' => 'create']) ?>" class="btn btn-primary">New Client</a>
</div>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Company</th>
            <th>Contact</th>
            <th>ABN</th>
            <th>Email</th>
            <th>Phone</th>
            <th>Address</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    <?php foreach ($clients as $c): ?>
        <tr>
            <td><?= $this->escapeHtml($c['company_name']) ?></td>
            <td><?= $this->escapeHtml($c['company_contact'] ?? '') ?></td>
            <td><?= $this->escapeHtml($c['abn'] ?? '') ?></td>
            <td><?= $this->escapeHtml($c['contact_email'] ?? '') ?></td>
            <td><?= $this->escapeHtml($c['contact_phone'] ?? '') ?></td>
            <td><?= $this->escapeHtml($c['address'] ?? '') ?></td>
            <td class="text-end">
                <a class="btn btn-sm btn-outline-secondary" href="<?= $this->url('clients', ['action' => 'edit', 'id' => $c['id']]) ?>">Edit</a>
                <a class="btn btn-sm btn-outline-danger" href="<?= $this->url('clients', ['action' => 'delete', 'id' => $c['id']]) ?>">Delete</a>
            </td>
        </tr>
    <?php endforeach; ?>
    </tbody>
</table>
